version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: reservations
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: q1w2e3
      MONGO_INITDB_DATABASE: sleepr
    ports:
      - 27017:27017
    volumes:
      - ./db/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
